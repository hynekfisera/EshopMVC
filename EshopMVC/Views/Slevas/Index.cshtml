@model IEnumerable<EshopMVC.Models.Sleva>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Hodnota)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JeProcentualni)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MinimalniPocet)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DatumZacatku)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DatumKonce)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Hodnota)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.JeProcentualni)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MinimalniPocet)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DatumZacatku)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DatumKonce)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SlevaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SlevaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SlevaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
